Shopping Cart API:

    //Recibe un parámetro de tipo Item y lo agrega al carrito
	-addItem(Item anItem); 
	
	
	/* Recibe un parámetro de tipo Item, 
	   Si el item existe en el carrito entonces lo elimina y devuelve True
	   Si el item no existe en el carrito devuelve False y muestra un mensaje */
	-public boolean removeItem(Item anItem);
	
	
	//Devuelve la suma de los precios de todos los items del carrito
	-getTotalPrice();
	
	
	//Devuelve una lista con todos los items que posee el carrito
	-getItems();
	
	
	/*Llama al método getTotalPrice()
	 Muestra un mensaje indicando la suma total de los precios de los items
	 llama al método emptyCart() para eliminar los items comprados */
	-buy();
	
	
	/*Vacia el carrito eliminando todos los items que tenga
	  Este método es llamado por el método buy*/
	-emptyCart();